# Put your real keys in .env (do NOT commit)
OPENAI_API_KEY=
USE_GOOGLE_VISION=false
GOOGLE_APPLICATION_CREDENTIALS=   # set only if using Google Vision
JWT_SECRET=replace_with_a_strong_secret_key
ENCRYPTION_KEY=replace_with_a_32_byte_base64_key   # Fernet key (base64) - optional for demo
